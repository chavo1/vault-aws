---
driver:
  name: terraform

provisioner:
  name: terraform

verifier:
  name: terraform
  systems:
    - name: basic
      backend: local
      attrs_outputs:
        vault: public_dns_vault

platforms:
  - name: terraform

suites:
  - name: default
